# PawZone — Обзор проекта

## Что это?

**PawZone** — интернет-магазин домашних животных на Laravel. Пользователи могут просматривать, покупать и управлять питомцами. Интерфейс полностью на узбекском языке.

---

## Технологический стек

| Слой | Технология |
|------|-----------|
| Backend | Laravel 12.x, PHP 8.2+ |
| Database | SQLite |
| Frontend CSS | Bootstrap 5.3.0 + Tailwind CSS 4.0.0 |
| Build tool | Vite 7.x + laravel-vite-plugin |
| HTTP client | Axios 1.11.0 |
| Fonts | Google Fonts: Inter, Poppins |

---

## Структура директорий

```
/PawZone
├── app/
│   ├── Http/
│   │   └── Controllers/
│   │       ├── PetController.php      # CRUD для питомцев
│   │       └── OrderController.php    # Оформление заказов
│   └── Models/
│       ├── Pet.php                    # Модель питомца
│       ├── Category.php               # Категории
│       ├── Order.php                  # Заказы
│       └── User.php                   # Пользователи
├── database/
│   ├── migrations/                    # 8 миграций
│   └── seeders/
│       ├── CategorySeeder.php         # 4 категории
│       └── PetSeeder.php              # 14 питомцев
├── resources/
│   └── views/
│       ├── layouts/app.blade.php      # Главный шаблон
│       ├── partials/
│       │   ├── header.blade.php       # Шапка/навигация
│       │   └── footer.blade.php       # Подвал
│       ├── components/
│       │   ├── alert.blade.php        # Уведомления
│       │   └── form-input.blade.php   # Поле формы
│       └── pets/
│           ├── index.blade.php        # Каталог питомцев
│           ├── show.blade.php         # Детальная страница
│           ├── create.blade.php       # Добавить питомца
│           └── edit.blade.php         # Редактировать питомца
├── routes/
│   └── web.php                        # Все маршруты
├── storage/app/public/images/pets/    # Загруженные фото
└── public/build/                      # Скомпилированные ассеты
```

---

## База данных

### Таблицы

**categories**
```
id | name | slug | timestamps
```

**pets**
```
id | category_id (FK) | name | price (decimal) | description | image | status | timestamps
status: 'available' | 'pending' | 'sold'
```

**orders**
```
id | pet_id (FK) | customer_name | customer_phone | status (default: 'new') | timestamps
```

### Сиды (начальные данные)

Категории:
- 🐱 Mushuklar (Кошки)
- 🐶 Itlar (Собаки)
- 🐦 Qushlar (Птицы)
- 🐟 Baliqlar (Рыбки)

Питомцев: 14 штук распределены по категориям

---

## Маршруты

```
GET  /                   → редирект на /pets
GET  /pets               → PetController@index    (каталог)
GET  /pets/create        → PetController@create   (форма добавления)
POST /pets               → PetController@store    (сохранить)
GET  /pets/{id}          → PetController@show     (детальная)
GET  /pets/{id}/edit     → PetController@edit     (форма редактирования)
PUT  /pets/{id}          → PetController@update   (обновить)
DELETE /pets/{id}        → PetController@destroy  (удалить)
POST /pets/{id}/buy      → OrderController@store  (купить)
```

---

## Функциональность

### Для пользователей
- Просмотр каталога питомцев
- Фильтрация по категории
- Детальная страница питомца
- Похожие питомцы (из той же категории)
- Форма заказа (имя + телефон) через модальное окно
- При заказе: статус питомца меняется `available → pending`

### Для администраторов
- Добавить питомца (с загрузкой фото, JPG/PNG/GIF, макс. 2MB)
- Редактировать питомца (с заменой фото)
- Удалить питомца (+ автоудаление файла изображения)

---

## Eloquent-связи

```php
Category → hasMany → Pet
Pet      → belongsTo → Category
Pet      → hasMany → Orders (cascade delete)
Order    → belongsTo → Pet
```

---

## Дизайн и UI

**Стиль:** Glassmorphism — полупрозрачные карточки, blur-эффекты

**Цветовая схема:**
- Основной: `#667eea → #764ba2` (фиолетовый)
- Акцент: `#f093fb → #f5576c` (розовый)
- Успех: `#43e97b → #38f9d7` (зелёный)
- Фон: `#1a1a2e` (тёмно-синий)

**Шрифты:** Inter (основной), Poppins (заголовки)

**Компоненты Blade:**
- `<x-alert>` — уведомления об успехе/ошибке
- `<x-form-input>` — переиспользуемое поле формы

---

## Конфигурация (.env)

```env
APP_NAME=PawZone
DB_CONNECTION=sqlite
SESSION_DRIVER=file
CACHE_STORE=file
QUEUE_CONNECTION=redis
MAIL_MAILER=log
```

---

## Запуск проекта

```bash
# Установка зависимостей
composer install
npm install

# Настройка БД
php artisan migrate --seed

# Запуск (все процессы сразу)
npm run dev
# = php artisan serve + vite + queue + logs
```

---

## Текущие задачи (app/tz.md)

Файл `app/tz.md` содержит todo-лист на узбекском. Проект в активной разработке.

---

## Возможные улучшения

### Срочно / Важно
- [ ] Авторизация для admin-функций (сейчас CRUD открыт всем)
- [ ] Пагинация в каталоге питомцев
- [ ] Валидация телефона при заказе
- [ ] Страница управления заказами для администратора

### Функциональность
- [ ] Корзина покупок (несколько товаров)
- [ ] Поиск по каталогу
- [ ] Сортировка (по цене, дате)
- [ ] Уведомления администратору о новых заказах
- [ ] История заказов для покупателя
- [ ] Статусы заказов с уведомлением (SMS/email)

### Технические
- [ ] Добавить аутентификацию (Laravel Breeze)
- [ ] Policy-доступ для admin-роутов
- [ ] API-эндпоинты (для мобильного приложения)
- [ ] Кэширование каталога
- [ ] Оптимизация изображений при загрузке
- [ ] Тесты (Feature + Unit)
- [ ] Переход на MySQL/PostgreSQL для продакшена

### UI/UX
- [ ] Skeleton-лоадер при загрузке
- [ ] Lazy loading для изображений
- [ ] Фильтры на стороне клиента (без перезагрузки страницы)
- [ ] Режим тёмной/светлой темы
- [ ] SEO-метатеги

---

*Последнее обновление: 2026-02-20*